/** Re-export dual-column virtual helpers (chip-library names kept for existing tests). */
export {
  chipLibraryRowPairs,
  visibleRowWindow,
  initialVisibleRowWindow,
  CHIP_LIBRARY_DEFAULT_ROW_HEIGHT,
  CHIP_LIBRARY_DEFAULT_VIEWPORT_HEIGHT,
  CHIP_LIBRARY_OVERSCAN,
  dualColumnRowPairs,
  DUAL_COLUMN_DEFAULT_ROW_HEIGHT,
  DUAL_COLUMN_DEFAULT_VIEWPORT_HEIGHT,
  DUAL_COLUMN_OVERSCAN,
  type VisibleRowWindow,
} from "./dualColumnVirtual";
